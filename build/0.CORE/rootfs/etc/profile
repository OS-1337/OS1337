# /etc/profile: system-wide .profile file for the shell (sh(1))
# and compatible shells (bash(1), ksh(1), ash(1), ...).

##	faster to type, DOS-alike commands
alias cls='clear'
alias dir='ls -ahl'
alias ding='echo -ne '\007''
### Create a Beep [will only make a system beep if output is redirected to a local or unused console]
alias unixtime='date +%s'
### Output Unixtime

## Network Diagnostics commands
function isup() { ping -a -b -c 1 -D "$@"; }
###	Single Ping attempt to a specific host which has to be specified i.e. "isup frogfind.com"
alias pub4='wget http://ip4only.me/api/ ; echo;'
### find public IPv4 adress
### see:    http://ip4only.me/
### NOTE:   Lack of IPv4 connectivity will result in a 404 error!
alias pub5='wget http://whatismyv6.com/api/ ; echo;'
### find public IPv4 or IPv6 based off OS & Network preference
### see:    http://whatismyv6.com/
### NOTE:   Lack of connectivity will result in a 404 error!
alias pub6='wget http://ip6only.me/api/ ; echo;'
### find public IPv6 adress
### see:    http://ip6only.me/
### NOTE:   Lack of IPv6 connectivity will result in a 404 error!

## Mapped Applications
function ssh() { dbclient "$@"; }
###	Dropbear SSH Client is mapped to SSH so that people can instantly ssh to a system.
